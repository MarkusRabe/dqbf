# Internal-only instructions (not for public repo)
INTERNAL.md
.claude/

# Local-only reference PDFs (copyright)
docs/references/local/*.pdf

# Generated scratch report (timestamped reports under docs/dev_reports/ ARE committed)
docs/site/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Rust
**/rust/**/target/
Cargo.lock

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp

# Benchmark archives & large downloads (keep download scripts, not payloads)
benchmarks/**/*.tar.gz
benchmarks/**/*.tar.bz2
benchmarks/**/*.zip
benchmarks/_downloads/
# Generated benchmark instances + manifests (commit generators only).
# Committed assets in the same dirs (.aag/.eqfob/.asm sources, .gitkeep,
# random_qbf .qdimacs reference set) are tracked explicitly.
benchmarks/train/**/build/
benchmarks/train/**/*.dqdimacs.gz
benchmarks/train/**/manifest.json

# Solver outputs / certificates
*.aag.out
*.cert
results/
*.log

# Jupyter
.ipynb_checkpoints/
third_party/
results/.bench_cache/
benchmarks/train/pec_circuits/miter/*.dqdimacs.gz
benchmarks/train/pec_circuits/miter/manifest.json
provers/frust/target/
